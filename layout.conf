# Identity of the layout. Everything else is derived from these.
# LAYOUT_NAME must match both the src/<LAYOUT_NAME>.keylayout filename and the
# name="..." attribute inside it; scripts/build.sh enforces that.

LAYOUT_NAME="Ukrainian - Mac PC"
BUNDLE_ID="com.jenkokov.keyboardlayout.ukrainian-mac-pc"
INTENDED_LANGUAGE="uk"

# Basename used for release archives (no spaces).
ARCHIVE_NAME="Ukrainian-Mac-PC"
